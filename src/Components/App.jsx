import * as React from 'react';
import '../style/main.scss';

import { Button } from './atoms/Button';

export const App = () => {
    return (
        <div>
            <Button>Test</Button>
        </div>
    );
};
